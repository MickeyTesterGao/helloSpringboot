<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper:根标签，namespace：命名空间，随便写，一般保证命名空间唯一 ，为了使用接口动态代理，这里必须是接口的全路径名-->
<mapper namespace="spring.hello.mapper.UserMapper">
    <!--插入数据-->
    <insert id="insertUser" parameterType="spring.hello.domain.User">
        INSERT INTO tb_user (
        user_name,
        password,
        name,
        age,
        sex,
        birthday,
        created,
        updated
        )
        VALUES
        (
        #{username},
        #{password},
        #{name},
        #{age},
        #{sex},
        #{birthday},
        now(),
        now()
        );
    </insert>
    <!--更新用户-->
    <update id="updateUser" parameterType="spring.hello.domain.User">
        UPDATE tb_user
        <trim prefix="set" suffixOverrides=",">
            <if test="username!=null">user_name = #{username},</if>
            <if test="password!=null">password = #{password},</if>
            <if test="name!=null">name = #{name},</if>
            <if test="age!=null">age = #{age},</if>
            <if test="sex!=null">sex = #{sex},</if>
            <if test="birthday!=null">birthday = #{birthday},</if>
            updated = now(),
        </trim>
        WHERE
        (id = #{id});
    </update>
    <!--删除用户-->
    <delete id="deleteUserById" parameterType="java.lang.Integer">
        delete from tb_user where id=#{id}
    </delete>
    <!--查询所有用户-->
    <select id="queryUserAll" resultType="spring.hello.domain.User">
        select * from tb_user
    </select>
    <!--查询用户-->
    <select id="queryUserById" resultType="spring.hello.domain.User">
        select * from tb_user where id = #{id}
    </select>

    <select id="login" resultType="spring.hello.domain.User">
        select * from tb_user where user_name = #{username} and password = #{password}
    </select>

</mapper>
